import { useMemo } from 'react';
import { DEFAULT_ANIMATION_DURATION, DEFAULT_ANIMATION_EASING } from '../components/bottomSheet/constants';
/**
 * Generate timing animation configs.
 * @default
 * - easing: Easing.out(Easing.exp)
 * - duration 500
 * @param configs overridable configs.
 */

export const useBottomSheetTimingConfigs = configs => {
  return useMemo(() => {
    const _configs = {
      easing: DEFAULT_ANIMATION_EASING,
      duration: DEFAULT_ANIMATION_DURATION
    };

    if (configs.easing) {
      _configs.easing = configs.easing;
    }

    if (configs.duration) {
      _configs.duration = configs.duration;
    }

    return _configs;
  }, [configs.duration, configs.easing]);
};
//# sourceMappingURL=useBottomSheetTimingConfigs.js.map