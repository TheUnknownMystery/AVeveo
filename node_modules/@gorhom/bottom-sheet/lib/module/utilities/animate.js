import { withSpring, withTiming } from 'react-native-reanimated';
import { ANIMATION_METHOD } from '../constants';
export const animate = (point, configs, velocity, onComplete) => {
  'worklet'; // detect animation type

  const type = 'duration' in configs || 'easing' in configs ? ANIMATION_METHOD.TIMING : ANIMATION_METHOD.SPRING;

  if (type === ANIMATION_METHOD.TIMING) {
    return withTiming(point, configs, onComplete);
  } else {
    // @ts-ignore
    configs.velocity = velocity;
    return withSpring(point, configs, onComplete);
  }
};
//# sourceMappingURL=animate.js.map