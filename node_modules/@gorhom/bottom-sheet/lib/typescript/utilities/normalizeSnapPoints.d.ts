/**
 * Converts snap points with percentage to fixed numbers.
 */
export declare const normalizeSnapPoints: (snapPoints: Array<number | string>, containerHeight: number, topInset: number) => number[];
